rootProject.name = 'SpringBootProdApplication'

pluginManagement {
    repositories {
        maven {url 'https://repo.spring.io/release'}
        mavenCentral()
        gradlePluginPortal()
    }
}

